
let initialState = {
    friends: [
        {id: 1, friend: 'Misha'},
        {id: 2, friend: 'Nastya'},
        {id: 2, friend: 'Kirill'}
    ]
};

const navbarReducer = (state = initialState, action) => {

    return state;
}

export default navbarReducer;